/**
 * Entry point for Cypress custom commands (implementations under `./custom-commands/`).
 *
 * Naming: imperative `VerbNoun` / `Visit…` for actions; `Subject…Should…` for checks so
 * `cy` chains read top-to-bottom like a short spec.
 */
import "./custom-commands";

export {};
