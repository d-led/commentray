/**
 * Entry point for Cypress custom commands (implementations under `./custom-commands/`).
 *
 * Naming: actions read as short phrases (`GoToStaticSiteHome`, `ChooseValueOfAngleSelect`,
 * `TypeTextInSearchField`, `Click…Of…`); checks use `…Should…` so `cy` chains read like sentences.
 */
import "./custom-commands";

export {};
