## More detail

This Markdown file is wired through `.commentray.toml` under `[static_site]` as `commentray_markdown`. It appears **below** the intro and GitHub link in the commentray pane.

You can point `commentray_markdown` at any repo-relative path, or leave it unset and rely on `intro` plus `github_url` only.
