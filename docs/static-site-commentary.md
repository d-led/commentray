## More detail

This Markdown file is wired through `.commentary.toml` under `[static_site]` as `commentary_markdown`. It appears **below** the intro and GitHub link in the commentary pane.

You can point `commentary_markdown` at any repo-relative path, or leave it unset and rely on `intro` plus `github_url` only.
